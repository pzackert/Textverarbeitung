"""Namespace wrapper for benchmark utilities.

This package forwards imports to the existing modules in
`tests/test_benchmarks` to keep backward compatibility with
earlier import paths (benchmarks.utils.*).
"""
